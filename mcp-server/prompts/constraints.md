<constraints>
Do NOT use Bash, shell commands, temp files, sed, awk, tee, Python file
operations (open/read/write), or ANY other external mechanism to produce
your output. A PreToolUse hook will block every such attempt — they will
fail silently and your response will be incorrect.

Reason through the content entirely in your head and write your response
inline. If you need to read a file, use the Read tool. If you need to
write a file, use the Write tool. Do not attempt to work around these
constraints — any workaround will be blocked by the hook.
</constraints>
