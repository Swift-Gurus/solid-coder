{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ISP Metrics",
  "description": "Schema for the 'ISP' key inside the unified review-output metrics object.",
  "type": "object",
  "required": ["width", "min_coverage", "cohesion_groups"],
  "properties": {
    "width": {
      "type": "object",
      "required": ["value"],
      "properties": {
        "value": { "type": "integer", "description": "ISP-1: total number of methods/properties declared in the protocol" },
        "additional-info": {}
      }
    },
    "min_coverage": {
      "type": "object",
      "required": ["value"],
      "properties": {
        "value": { "type": "integer", "minimum": 0, "maximum": 100, "description": "ISP-2: minimum meaningful implementation coverage across all conformers (0-100)" },
        "additional-info": {}
      }
    },
    "cohesion_groups": {
      "type": "object",
      "required": ["value"],
      "properties": {
        "value": { "type": "integer", "description": "ISP-3: number of disjoint method-usage groups across conformers" },
        "additional-info": {}
      }
    }
  }
}
