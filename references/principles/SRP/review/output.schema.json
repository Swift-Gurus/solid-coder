{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SRP Metrics",
  "description": "Schema for the 'SRP' key inside the unified review-output metrics object.",
  "type": "object",
  "required": ["verb_count", "cohesion_groups", "stakeholder_count"],
  "properties": {
    "verb_count": {
      "type": "object",
      "required": ["value"],
      "properties": {
        "value": { "type": "integer", "description": "SRP-1/SRP-3: total number of distinct verbs (responsibilities) in the unit" },
        "additional-info": { "description": "Optional verb table: [{method, verb}]" }
      }
    },
    "cohesion_groups": {
      "type": "object",
      "required": ["value"],
      "properties": {
        "value": { "type": "integer", "description": "SRP-2: number of disjoint cohesion groups (sets of methods sharing instance variables)" },
        "additional-info": { "description": "Optional group detail: {groups: [{name, variables, methods}], method_variable_table: [{method, variables}]}" }
      }
    },
    "stakeholder_count": {
      "type": "object",
      "required": ["value"],
      "properties": {
        "value": { "type": "integer", "description": "SRP-1/SRP-3: number of distinct stakeholders served by the unit" },
        "additional-info": { "description": "Optional stakeholder table: [{verb, stakeholder}]" }
      }
    }
  }
}
