{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "true  # token-doc regen disabled — re-enable by restoring: jq -r '.tool_input.file_path // empty' | grep -q '/references/' && python3 \"$CLAUDE_PROJECT_DIR/scripts/regen-token-docs.py\" || true"
          }
        ]
      }
    ]
  }
}
